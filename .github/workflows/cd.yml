name: Continuos Delivery

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

jobs:
  ci:
    uses: husseymarcos/ingsis-workflows/.github/workflows/cd-packages.yml@main