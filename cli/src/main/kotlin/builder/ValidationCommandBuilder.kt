package builder

import command.Command
import command.ValidationCommand

class ValidationCommandBuilder: CommandBuilder {

  override fun build(arguments: String): Command {
    fun

    return ValidationCommand()
  }

}
